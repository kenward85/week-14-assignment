import React from 'react';
import Card from './shared/Card.jsx';

function About() {
  return (
    <Card title="About">
      <p>
        This SPA demonstrates React Router, controlled forms, effects, callbacks,
        conditional rendering, and persistence via localStorage.
      </p>
    </Card>
  );
}

export default About;
